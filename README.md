Вот краткий и информативный README.md для вашего GitHub-репозитория:

```markdown
# Анализ цикличности акций (ACF/PACF) на примере VTBR

Этот репозиторий содержит R-скрипты для статистического анализа временных рядов акций с помощью автокорреляции (ACF) и частной автокорреляции (PACF). Основная цель — поиск скрытых циклов и подтверждение торговых гипотез (например, 21-дневный цикл).

## 📊 Скрипты

### 1. `acf_test.R`
Анализ общей цикличности и проверка перегретости актива:
- Загрузка и подготовка данных
- STL-декомпозиция (тренд + сезонность + остатки)
- Расчет ACF до 30 лагов с определением статистической значимости
- Технические индикаторы: RSI, Stochastic, Bollinger Bands, MACD
- Проверка гипотезы о 21-дневном цикле

### 2. `pacf_test.R`
Поиск скрытых импульсов и новых драйверов роста:
- Расчет PACF до 30 лагов
- Определение порога значимости (95% доверительный интервал)
- Специальная проверка 21-го лага для выявления нового импульса
- Сохранение результатов в CSV

## 📈 Входные данные

Скрипты ожидают CSV-файл со следующей структурой:
- `Date` — дата в формате YYYY-MM-DD
- `Close` — цена закрытия
- (опционально) `Open`, `High`, `Low`, `Volume`

## 🔍 Что ищем

- **ACF** — показывает общую цикличность и моментум (инерцию движения)
- **PACF** — выявляет точку, где заканчивается инерция и начинается новый импульс
- **21-й лаг** — гипотеза о месячном цикле (21 торговый день)

## 🚀 Использование

1. Укажите путь к вашему CSV-файлу в переменной `file_name`
2. Запустите скрипт в R или RStudio
3. Получите:
   - Графики ACF/PACF с "синей зоной" шума
   - Таблицу значимых лагов
   - Вердикт по 21-му дню

## 📦 Зависимости

Установите пакеты перед запуском:
```r
install.packages(c("quantmod", "forecast", "TTR", "ggplot2", "lubridate", "readr", "xts"))
```

## 📌 Пример вывода

```
--- ПРОВЕРКА ЦИКЛА (21 ДЕНЬ) ---
  Lag PACF_Value Is_Significant
1  21      0.123           TRUE

ВЫВОД: На 21-м дне есть НОВЫЙ импульс (драйвер роста).
```

## 📁 Результаты

Скрипты автоматически сохраняют:
- `vtbr_pacf_results.csv` — таблица значений PACF
- `acf_results.csv` — таблица значений ACF

## ⚠️ Примечание

Данный код предназначен для исследовательских целей и не является инвестиционной рекомендацией.
```
